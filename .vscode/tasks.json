{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build:debug",
            "command": "zig",
            "args": ["build-exe", "-Doptimize=Debug", "-femit-bin=zig-out/bin/debug-hello", "hello.zig"]

        },
    ]
}
